VITE_API_BASE_URL={{ api_base_url }}
VITE_AUTH_DISABLED={% if include_auth %}false{% else %}true{% endif %}
VITE_KEYCLOAK_URL={{ keycloak_url | default('http://localhost:8080') }}
VITE_KEYCLOAK_REALM={{ keycloak_realm | default('master') }}
VITE_KEYCLOAK_CLIENT_ID={{ keycloak_client_id | default(project_slug) }}
VITE_MSW_ENABLED=false
