# reliability_connection_pool — Node.
#
# Injects the pool helper import above the Prisma client initialisation
# block. The base template ships `FORGE:PRISMA_CLIENT_INIT` in
# `src/lib/prisma.ts` (1.0.0a2+).

- target: src/lib/prisma.ts
  marker: FORGE:PRISMA_CLIENT_INIT
  position: before
  zone: generated
  snippet: "import { databaseUrlWithPool } from './prisma-pool.js';"
